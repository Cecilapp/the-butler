---
label: Page
hide_body: false
display_field: title
fields:
- name: common
  type: include
  config: {}
  template: common
  label: Common
- type: select
  name: menu
  config:
    source:
      type: simple
    options:
    - main
  label: Menu
